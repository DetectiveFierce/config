# hyprpaper configuration
# Wallpapers are managed dynamically by ~/.config/hypr/scripts/wallpaper-rotator.sh
# The rotator script handles preloading and setting wallpapers every 10 minutes

# Enable IPC for hyprctl commands from the rotator script
ipc = true

# Disable splash text/quote at bottom of screen
splash = false
